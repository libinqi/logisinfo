<%
stores.forEach(function(store){
%>
<div class="info_item">
    <div class="info_title">
        <div class="w210 ln_padding pull-left">
            <span class="pull-left"><strong><%=store.storeName%></strong></span>
            <span class="clearfix"></span>
        </div>
        <!--<div class="w100 pull-left"><small>最新订单：<span class="text-primary">5</span></small></div>-->
        <div class="w220 pull-left"><small>更新时间：<%= store.updatedAt %></small></div>
        <div class="w140 pull-right">
            <small class="dropdown">信息有效期：
                <a href="#" class="dropdown-toggle"
                   data-toggle="dropdown"><%= store.valid %></a>
            </small>
        </div>
        <div class="chaozuo pull-right">
            <!--<div class="shouqi pull-left"><a href="#"><img src="images/shouqi.png"></a></div>-->
            <!--<div class="delet pull-right"><a href="javascript:void(0)" onclick="deleteStore('<%= store.id %>');return false;"><img src="images/delet.png"></a></div>-->
        </div>
        <div class="clearfix"></div>
    </div>
    <div class="info_content">
        <div class=" pull-left w145 tn_padding ln_padding bn_padding">
            <a href="#"><img src="<%=store.image%>" width="135" height="100"></a>
        </div>
        <div class="pull-left w530 tn_padding rn_padding ln_padding lianxi">
            <p>面积：<span class="text-primary"><%=store.storeArea%>㎡</span> | 经营范围：<span class="text-primary"><%=store.businessScope%></span></p>
            <p>仓库类型：<span class="text-primary"><%=store.storeType%></span> | 联系方式：<span class="text-primary"><%= store.phone %></span>
                <span class="text-primary"><%= store.tel %></span>
                <span class="text-primary">    <%= store.contact %></span></p>
            <p>仓库地址：<%= store.province %><%= store.city %><%= store.area %><%= store.address %></p>
        </div>

        <div class="pull-right w80 tl_padding">
            <a href="http://login.jt56.org/Api/Interface/goToInfo?type=store&euuid=<%= store.eId %>&infoid=<%= store.id %>"
               target="_blank" class="btn btn-info">查看</a>
        </div>
        <div class="clearfix"></div>
    </div>
</div>
<% }) %>
<% if(pages > 1){ %>
<div class="col-lg-5 col-sm-offset-4">
    <ul class="pagination" current_page='<%= current_page %>'>
        <%
            function removeURLParameter(url, parameter) {
                var urlparts = url.split('?');
                if (urlparts.length >= 2) {
                    var prefix = encodeURIComponent(parameter) + '=';
                    var pars = urlparts[1].split(/[&;]/g);
                    for (var i = pars.length; i-- > 0;)
                        if (pars[i].lastIndexOf(prefix, 0) !== -1)
                            pars.splice(i, 1);
                    url = urlparts[0] + '?' + pars.join('&');
                    return url;
                } else {
                    return url;
                }
            }
            base = removeURLParameter(base, 'page');
            var base_url = base + (base.indexOf('?') < 0 ? '?' : '&') + 'page=';
        %>
        <% if (current_page == 1) { %>
        <li class='disabled'><a>«</a></li>
        <% } else { %>
        <li><a href="<%= base_url %>1">«</a></li>
        <% } %>

        <%
            var page_start = current_page - 2 > 0 ? current_page - 2 : 1;
            var page_end = page_start + 4 >= pages ? pages : page_start + 4;
        %>

        <% if (page_start > 1) { %>
        <li><a>...</a></li>
        <% } %>

        <% for(var i = page_start; i <= page_end; i++) { %>
        <% if (i === current_page) { %>
        <li><a><%= i %></a></li>
        <% } else { %>
        <li><a href='<%= base_url + i %>'><%= i %></a></li>
        <% } %>
        <% } %>

        <% if (page_end < pages ) { %>
        <li><a>...</a></li>
        <% } %>

        <% if (current_page == pages) { %>
        <li class='disabled'><a>»</a></li>
        <% } else { %>
        <li><a href='<%= base_url + pages %>'>»</a></li>
        <% } %>
    </ul>
</div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        var $nav = $('.pagination');
        var current_page = $nav.attr('current_page');
        if (current_page) {
            $nav.find('li').each(function () {
                var $li = $(this);
                var $a = $li.find('a');
                if ($a.html() == current_page) {
                    $li.addClass('active');
                    $a.removeAttr('href');
                }
            });
        }
    });
</script>
<% } %>