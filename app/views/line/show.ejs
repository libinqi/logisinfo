<div class="page-header">
    <h2 class="id"><%= line.id %></h2>

    <h3 id="js-overview">专线信息 <a href="/line/edit/<%= line.id %>" class="btn pull-right">编辑专线</a>
        <a href="/line" class="btn pull-right">返回</a></h3>
</div>
<div class="row">
    <% for(var i in line) { %>
    <div class="col-sm-6 col-md-3">
        <div class="thumbnail">
            <h3><%= i %></h3>
            <i class="glyphicon glyphicon-list-alt"></i><span class="<%= i %>"><%= line[i] %></span>
        </div>
    </div>
    <% } %>
    <div>