<%
lines.forEach(function(line){
%>
<div class="info_item">
    <div class="info_title">
        <div class="w350 ln_padding pull-left">
            <span class="pull-left"><%= line.sProvince + line.sCity %></span>
            <% if(line.lineTypeCode == 1){ %>
            <span class="zhida"><img src="/images/zhida.png"/></span>
            <% }else{ %>
            <span class="zhida"><img src="/images/wangfan.png"/></span>
            <% } %>
            <span id="per_ecity" class="pull-left"><%= line.eProvince + line.eCity %></span>
            <% if(line.isDirect == 1){ %>
            <span id="per_is_direct" class="pull-left">(直达)</span>
            <% }else{ %>
            <span id="per_is_direct" class="pull-left">(中转)</span>
            <% } %>
            <span class="clearfix"></span>
        </div>
        <!--<div class="w100 pull-left">-->
        <!--<small>最新订单：<span class="text-primary">5</span></small>-->
        <!--</div>-->
        <div class="w200 pull-left">
            <small>更新时间：<%= line.updatedAt %></small>
        </div>
        <div class="w140 pull-right">
            <small class="dropdown">信息有效期：
                <a href="#" class="dropdown-toggle"
                   data-toggle="dropdown"><%= line.valid %></a>
            </small>
        </div>

        <div class="chaozuo pull-right">
            <!--<div class="shouqi pull-left"><a href="#"><img src="images/shouqi.png"></a></div>-->
            <div class="delet pull-right">
                <!--<a href="/line/delete/<%= line.id %>"><img src="images/delet.png"></a>-->
                <!--<a href="javascript:void(0)" onclick="deleteLine('<%= line.id %>');return false;"><img-->
                            <!--src="/images/delet.png"></a>-->
            </div>
        </div>
        <div class="clearfix"></div>
    </div>

    <div class="info_content">
        <div class=" pull-left w145 tn_padding ln_padding bn_padding">
            <a href="#"><img src="<%= line.image %>" width="135" height="100"></a>
        </div>
        <div class="pull-left w350 tn_padding rn_padding ln_padding lianxi">
            <p>始发地址：<span><%= line.startAddress %></span></p>

            <p>联系方式：<span class="text-primary"><%= line.startPhone %></span>
                <span class="text-primary"><%= line.startTel %></span>
                <span class="text-primary">    <%= line.startContact %></span></p>

            <p>到达地址：<span><%= line.endAddress %></span></p>

            <p>联系方式：<span class="text-primary"><%= line.endPhone %></span>
                <span class="text-primary"><%= line.endTel %></span>
                <span class="text-primary">    <%= line.endContact %></span></p>

        </div>
        <div class="pull-left w145 ln_padding tn_padding xijie">
            <div class="w65 text-right pull-left">重货价格：</div>
            <div class="pull-left"><%= line.heavyCargoPrice %></div>
            <div class="w65 text-right pull-left">泡货价格：</div>
            <div class="pull-left"><%= line.foamGoodsPrice %></div>
            <span class="w65 text-right pull-left">运输方式：</span>

            <div class="pull-left"><%= line.modeTransport %></div>
            <span class="w65 text-right pull-left">发车班次：</span>

            <div class="pull-left"><%= line.transRate %></div>
            <span class="w65 text-right pull-left">在途时间：</span>

            <div class="pull-left"><%= line.transTimeText %></div>
        </div>
        <div class="pull-right w80 tl_padding">
            <a href="http://login.jt56.org/Api/Interface/goToInfo?type=line&euuid=<%= line.eId %>&infoid=<%= line.id %>"
               target="_blank" class="btn btn-info">查看</a>
        </div>
        <div class="clearfix"></div>
    </div>
</div>
<% }) %>
<% if(pages > 1){ %>
<div class="col-lg-5 col-sm-offset-4">
    <ul class="pagination" current_page='<%= current_page %>'>
        <%
            function removeURLParameter(url, parameter) {
                var urlparts = url.split('?');
                if (urlparts.length >= 2) {
                    var prefix = encodeURIComponent(parameter) + '=';
                    var pars = urlparts[1].split(/[&;]/g);
                    for (var i = pars.length; i-- > 0;)
                        if (pars[i].lastIndexOf(prefix, 0) !== -1)
                            pars.splice(i, 1);
                    url = urlparts[0] + '?' + pars.join('&');
                    return url;
                } else {
                    return url;
                }
            }
            base = removeURLParameter(base, 'page');
            var base_url = base + (base.indexOf('?') < 0 ? '?' : '&') + 'page=';
        %>
        <% if (current_page == 1) { %>
        <li class='disabled'><a>«</a></li>
        <% } else { %>
        <li><a href="<%= base_url %>1">«</a></li>
        <% } %>

        <%
            var page_start = current_page - 2 > 0 ? current_page - 2 : 1;
            var page_end = page_start + 4 >= pages ? pages : page_start + 4;
        %>

        <% if (page_start > 1) { %>
        <li><a>...</a></li>
        <% } %>

        <% for(var i = page_start; i <= page_end; i++) { %>
        <% if (i === current_page) { %>
        <li><a><%= i %></a></li>
        <% } else { %>
        <li><a href='<%= base_url + i %>'><%= i %></a></li>
        <% } %>
        <% } %>

        <% if (page_end < pages ) { %>
        <li><a>...</a></li>
        <% } %>

        <% if (current_page == pages) { %>
        <li class='disabled'><a>»</a></li>
        <% } else { %>
        <li><a href='<%= base_url + pages %>'>»</a></li>
        <% } %>
    </ul>
</div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        var $nav = $('.pagination');
        var current_page = $nav.attr('current_page');
        if (current_page) {
            $nav.find('li').each(function () {
                var $li = $(this);
                var $a = $li.find('a');
                if ($a.html() == current_page) {
                    $li.addClass('active');
                    $a.removeAttr('href');
                }
            });
        }
    });
</script>
<% } %>